import DropSection from "@/components/DropSection";

export const metadata = {
  title: "The Founder's Drop — Stop the World",
  description: "500 pieces. No more.",
};

export default function DropPage() {
  return <DropSection />;
}