import BooksSection from "@/components/BooksSection";

export const metadata = {
  title: "Books — Stop the World",
  description: "Two books, two depths. From Unrealised Books.",
};

export default function BooksPage() {
  return <BooksSection />;
}